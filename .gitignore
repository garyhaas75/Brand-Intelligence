# Environment — never commit secrets
.env
.env.local
.env.*.local

# Scraped data — lives on the server volume, not in git
data/*.json
# Taxonomy is static reference data (updated quarterly) — commit it so Railway has it
!data/shopify_taxonomy.json


# Logs
logs/
*.log

# Node
node_modules/
npm-debug.log*

# dashboard/dist is intentionally tracked (committed locally, Railway skips build step)
dashboard/node_modules/

# Credentials — never commit OAuth key files
credentials/

# Claude Code local config
.claude/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
.githooks/qa-critic.log
